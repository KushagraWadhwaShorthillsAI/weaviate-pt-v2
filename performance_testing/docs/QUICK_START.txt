================================================================================
🚀 PARALLEL TESTING - QUICK START GUIDE
================================================================================

📁 Location: performance_testing/parallel_testing/

================================================================================
⚡ QUICK RUN (30 second test)
================================================================================

cd /Users/shtlpmac_002/Downloads/nthScaling/performance_testing/parallel_testing
source ../../venv/bin/activate
locust -f locustfile_vector.py --users 10 --spawn-rate 2 --run-time 30s --headless

================================================================================
🔥 FULL TEST SUITE (All 5 types, 5 minutes each)
================================================================================

cd /Users/shtlpmac_002/Downloads/nthScaling/performance_testing/parallel_testing
source ../../venv/bin/activate
./run_parallel_tests.sh

================================================================================
🎯 INDIVIDUAL TESTS
================================================================================

# Vector Search
locust -f locustfile_vector.py --users 100 --spawn-rate 5 --run-time 5m --headless

# BM25 Search
locust -f locustfile_bm25.py --users 100 --spawn-rate 5 --run-time 5m --headless

# Hybrid Alpha=0.1
locust -f locustfile_hybrid_01.py --users 100 --spawn-rate 5 --run-time 5m --headless

# Hybrid Alpha=0.9
locust -f locustfile_hybrid_09.py --users 100 --spawn-rate 5 --run-time 5m --headless

# Mixed
locust -f locustfile_mixed.py --users 100 --spawn-rate 5 --run-time 5m --headless

================================================================================
📊 COMPARISON WITH MULTI-COLLECTION
================================================================================

# 1. Run multi-collection (existing)
cd ../multi_collection
locust -f locustfile_vector.py --users 100 --run-time 5m --headless --html multi_vector.html

# 2. Run parallel (new)
cd ../parallel_testing
locust -f locustfile_vector.py --users 100 --run-time 5m --headless --html parallel_vector.html

# 3. Compare reports
open multi_vector.html parallel_vector.html

================================================================================
🔧 REGENERATE QUERIES (if needed)
================================================================================

cd /Users/shtlpmac_002/Downloads/nthScaling/performance_testing/parallel_testing
source ../../venv/bin/activate
python generate_parallel_queries.py --search-types all --limits 10 50 100 150 200

================================================================================
✅ VALIDATION
================================================================================

cd /Users/shtlpmac_002/Downloads/nthScaling/performance_testing/parallel_testing
source ../../venv/bin/activate
./quick_test.sh

================================================================================
📚 FILES OVERVIEW
================================================================================

generate_parallel_queries.py    → Creates query files (uses cached embeddings)
locustfile_vector.py            → Pure vector search (9 parallel requests)
locustfile_bm25.py              → BM25 keyword search (9 parallel requests)
locustfile_hybrid_01.py         → Hybrid α=0.1 (9 parallel requests)
locustfile_hybrid_09.py         → Hybrid α=0.9 (9 parallel requests)
locustfile_mixed.py             → Mixed search types (9 parallel requests)
run_parallel_tests.sh           → Automated test runner (all 5 types)
quick_test.sh                   → Validation script
queries/                        → 25 query files (40 sets × 9 queries each)
README.md                       → Comprehensive documentation
IMPLEMENTATION_SUMMARY.md       → Technical details & implementation notes
QUICK_START.txt                 → This file

================================================================================
🎯 WHAT'S DIFFERENT FROM MULTI-COLLECTION?
================================================================================

Multi-Collection Approach:
  • 1 GraphQL query with 9 sub-queries
  • Weaviate processes internally
  • Time = Sequential or optimized internal processing

Parallel Approach (THIS):
  • 9 separate HTTP requests sent simultaneously
  • Client-side parallelization using gevent
  • Time = max(slowest_collection) + HTTP overhead

Expected Result:
  • Parallel should be FASTER if bottleneck is slowest collection
  • Total time ≈ SongLyrics response time (~800ms)
  • Speedup: 2-3x faster than multi-collection

================================================================================
📊 QUERIES GENERATED
================================================================================

Total Files: 25
Total Query Sets: 30 per file
Total Queries per Set: 9 (one per collection)

Breakdown:
  • Vector: 5 files (10, 50, 100, 150, 200 limits)
  • BM25: 5 files (10, 50, 100, 150, 200 limits)
  • Hybrid 0.1: 5 files (10, 50, 100, 150, 200 limits)
  • Hybrid 0.9: 5 files (10, 50, 100, 150, 200 limits)
  • Mixed: 5 files (10, 50, 100, 150, 200 limits)

Total Individual Queries: 30 × 9 × 25 = 6,750 queries!

================================================================================
🚨 TROUBLESHOOTING
================================================================================

Problem: "No module named 'gevent'"
Solution: source ../../venv/bin/activate && pip install gevent

Problem: "embeddings_cache.json not found"
Solution: cd .. && python generate_all_queries.py --type multi --search-types vector

Problem: "All requests timing out"
Solution: Check Weaviate is running: curl http://20.161.96.75/v1/.well-known/ready

Problem: "Queries not loading"
Solution: python generate_parallel_queries.py --search-types all

================================================================================
🎓 KEY CONCEPTS
================================================================================

Gevent Greenlets:
  • Async I/O for parallel HTTP requests
  • Lightweight (not OS threads)
  • Non-blocking network operations
  • Built-in Locust support

Parallel Execution:
  1. Pick random query set (30 options)
  2. Spawn 9 greenlets (one per collection)
  3. Send all 9 HTTP requests simultaneously
  4. Wait for ALL to complete (30s timeout)
  5. Report total time to Locust

Error Handling:
  • 30s timeout per parallel batch
  • Partial success tracking (e.g., 7/9 succeeded)
  • Per-collection error metrics
  • Graceful degradation

================================================================================
📈 METRICS TO TRACK
================================================================================

Response Time:
  • Average: Mean time for 9 parallel requests
  • Median: 50th percentile
  • 95th Percentile: 95% completed under X ms
  • Max: Slowest parallel batch

Throughput:
  • Requests/sec: Parallel batches per second
  • Success Rate: % of batches with all 9 successful

Per-Collection:
  • Individual response times
  • Failure rates per collection
  • Identify slow collections

================================================================================
✅ READY TO TEST!
================================================================================

Start here:
  1. Validate setup: ./quick_test.sh
  2. Quick test (30s): locust -f locustfile_vector.py --users 10 --run-time 30s --headless
  3. Full suite: ./run_parallel_tests.sh

Good luck! 🚀
================================================================================

